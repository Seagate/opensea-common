#!/usr/bin/expect -f

set timeout 10

set prog [lindex $argv 0]

spawn $prog

expect "Enter password:"
send "mypassword"

# wait for the program to finish and print everything
expect eof

catch wait result
set exit_status [lindex $result 3]

exit $exit_status