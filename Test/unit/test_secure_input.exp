#!/usr/bin/expect -f

exp_internal 1
log_user 1

set timeout 30

set prog [lindex $argv 0]

spawn {*}$prog

expect {
    -re "Enter password.*" {
        send "mypassword\r"
    }
    timeout {
        puts "TIMEOUT waiting for prompt"
        exit 1
    }
}

expect eof